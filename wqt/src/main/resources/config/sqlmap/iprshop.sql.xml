<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iprShop"> 
      
       <!--查询知识产权出售商品-->
	   <select id="selectIprShopPage" 
		        parameterType="com.wqt.user.pojo.UIprShop" 
		        resultType="com.wqt.user.pojo.UIprShop">
		    select 
             a.*
             from v_ipr_shop a
             where 1=1
			<if test="null!= shopid and ''!= shopid">
			    and  a.shopid=#{shopid}
			</if>
			<if test="null!= shopcode and ''!= shopcode">
			    and  a.shopcode=#{shopcode}
			</if>
			<if test="null!= shopkindcode and ''!= shopkindcode">
			   and  a.shopkindcode=#{shopkindcode}
			</if>
			<if test="null!= shopuser and ''!= shopuser">
			   and  a.shopuser=#{shopuser}
			</if>
			<if test="null!= shopstate and ''!= shopstate">
			   and  a.shopstate=#{shopstate}
			</if>
			order by shopdeveloptime desc
	   </select>
</mapper>